# Curatorr Release Announcement

Curatorr `0.1.49` is out.

Highlights:

- **Release-automation hotfix** — analyzer image reuse now falls back to the nearest existing Docker Hub tag instead of assuming the immediately previous release always published successfully.
