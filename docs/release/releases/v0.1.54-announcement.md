# Curatorr Release Announcement

Curatorr `0.1.54` is out.

Highlights:

- **Database migration hardened** — upgrading from an older install no longer crashes on startup due to partial indexes referencing columns that hadn't been migrated yet.
- **Windows desktop silent crash fixed** — startup failures now show an error dialog rather than the app silently disappearing.
