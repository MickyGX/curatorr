# Curatorr v0.1.14

## Summary

- Refined the day-theme styling across filter controls, panel headers and bodies, discover titles, and Plex modals so the light theme now reads consistently instead of mixing dark-theme surfaces into key UI elements.

## Upgrade Notes

- Rebuild and redeploy Curatorr so the updated day-theme styles are included in the running image.
- Hard refresh the browser after upgrading so the refreshed CSS replaces cached assets.
