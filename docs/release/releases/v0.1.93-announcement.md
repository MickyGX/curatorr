# Curatorr Release Announcement

Curatorr `0.1.93` is out.

## Highlights

- **Clean production audit** - refreshed the Express dependency stack so `npm audit --omit=dev --audit-level=high` reports no vulnerabilities.
- **Patched transitive packages** - updates include `express`, `body-parser`, `path-to-regexp`, and `qs`.
- **No behavior changes** - this is a security dependency release only.

## Links

- GitHub release: {{RELEASE_URL}}
- Docker Hub app image: `{{APP_IMAGE}}`
- Release notes: `docs/release/releases/v0.1.93.md`
