# Curatorr Release Announcement

Curatorr `0.1.50` is out.

Highlights:

- **Docker hotfix** — this release restores `wget` in the runtime image so existing Curatorr compose healthchecks work again and the container no longer gets restarted as unhealthy.
